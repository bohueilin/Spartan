<?xml version="1.0" encoding="utf-8"?>
<!-- FileProvider scope for user-initiated export sharing ONLY: a single cache subdirectory,
     cleared before each export. Nothing else in app storage is reachable through this provider. -->
<paths>
    <cache-path name="exports" path="exports/" />
</paths>
